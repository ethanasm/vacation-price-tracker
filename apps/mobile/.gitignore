# Expo / RN build + cache output
.expo/
dist/
dist-web/
web-build/
coverage/

# Native prebuild output (regenerated by `expo prebuild` / `expo run:*`)
/ios
/android

# Metro / Babel caches
.metro-health-check*
